<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EmpExprMapper">


    <insert id="saveUserExper">
        insert into emp_expr(emp_id,company,job,begin,end)  values
--        动态sql：foreach标签
--         collectionsh:遍历的集合 item:集合中的元素 separator:分隔符 close:结束符
--        set 标签会自动生成set关键字，会去掉多余的逗号

        <foreach collection="list" item="expr" separator="," >
            (#{expr.empId},#{expr.company},#{expr.job},#{expr.begin},#{expr.end})
        </foreach>
    </insert>
    <delete id="remove">
        delete from emp_expr where emp_id in
        <foreach collection="list" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
</mapper>